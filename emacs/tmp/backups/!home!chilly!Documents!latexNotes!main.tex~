
\documentclass[11pt]{article}
\usepackage{amsfonts, amssymb, amsmath}
\usepackage{float}              % For using H with table
\parindent 0px
\pagestyle{empty}                % To turn off page numbers
\begin{document}

This is a math equation ${(x+1)}^3 = 5 - 2$

This is a math equation block $(x+1) = 5x \sin x$

For subscripts:

$ x_{2_3} $

\[a_0, a_1, a_2 \ldots a_n\]

For greek letters:

$\pi$ and $\Pi$

$\alpha$ and $\beta$

For functions:
\[ y = \sin x\]
\[ y = \tan x\]
\[ y = \csc \theta\]
\[ y = \sin^{-1} \theta\]
\[ y = \arcsin \theta\]
\[ y = \arcsin \theta\]
\[ y = \log x\]
\[ \log_{e} x \]
\[ \ln x \]

For roots:
\[ \sqrt[3]{2} \]
\[ \sqrt{1 + \sqrt{x}} \]


For fractions, $ \frac{2}{3} $, $ \frac{1}{x}$
with display style, $ \displaystyle \frac{2}{3} $, $ \displaystyle \frac{1}{x}$

For complex combinations:
\[\frac{\sqrt{x + 1}}{x}\]
\[\frac{\sqrt{x}}{1 + \frac{1}{x}}\]

For statements:\\[6pt]
The distributive property states that $a(b+c) = ab + ac$ $\forall$ $a,b \in \mathbb{R}$\\[2pt]
The equivalence class of $a$ is $a$.\\[2pt]
The set $A$ is given by $\{1,2,3\}$\\
For brackets:
\[\left( \frac{1}{x} \right)\]
\[\left\{ \frac{1}{x} \right\}\]
\[\left[ \frac{1}{x} \right]\]
\[\left\langle \frac{1}{x} \right\rangle\]
\[\left| \frac{1}{x+1} \right|\]
\[\left. \frac{dy}{dx} \right| _{x = 1}\]

Basic tables:\\[6pt]
\begin{tabular}{|c|cccc|}
  % c stands for center and no of c's gives you no of columns
  % options can be c, l, r and p{no}(paragraph =no is needed=)
  \hline
  $x$ & 1 & 2 & 3 & 4 \\ \hline
  $fx$ & 1 & 2 & 3 & 4 \\ \hline
\end{tabular}

\begin{table}[H]
  \centering                    % To center them
  \def\arraystretch{1.5}        % Top Bottom Paddings
  \begin{tabular}{|c|cccc|}
    \hline
    $x$ & 1 & 2 & 3 & 4 \\ \hline
    $fx$ & $\frac{1}{2}$ & 2 & 3 & 4 \\ \hline
  \end{tabular}
  \caption{ These values represent a function's respective values.}
\end{table}

\begin{table}[H]
  \def\arraystretch{1.5}        % Top Bottom Paddings
  \centering                    % To center them
  \begin{tabular}{|c|p{3in}|}
    \hline
    Sentence & The main reason we use p is so that it can accomodate large texts and doesnt let the text flow outside the table.\\ \hline
  \end{tabular}

  \caption{For long sentences.}
\end{table}

Arrays:
\begin{align}
  % To align at =, use &
5x^2 - 3 &= x+2\\
5y^2 &= y+3	
\end{align}

\begin{align*}
\text{Equations to ignore numbering}\\
5x^2 - 3 &= x+2\\
5y^2 &= y+3	
\end{align*}

\begin{align}
 \text{Continue numbering}\\
5x^2 - 3 &= x+2\\
5y^2 &= y+3	
\end{align}


Lists:

Example 1:
\begin{itemize}
\item pencil	
\item calculator
  \begin{enumerate}
    \item Homework
    \item Notes
  \end{enumerate}
\item eraser
  \begin{itemize}
    \item Homework
    \item Notes
  \end{itemize}
\item ruler
\end{itemize}

Example 2:
\begin{enumerate}
\item pencil	
\item calculator
\item notebook
  \begin{enumerate}
    \item Homework
    \item[] Notes               % To remove the numbering
  \end{enumerate}
\item eraser
\item ruler
\end{enumerate}

\end{document}
